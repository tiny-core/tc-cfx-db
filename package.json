{
  "name": "@tc/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --target=node --format=cjs --minify --outfile=dist/server.js"
  },
  "dependencies": {
    "mysql2": "^3.11.0"
  },
  "license": "LGPL-3.0-or-later"
}
